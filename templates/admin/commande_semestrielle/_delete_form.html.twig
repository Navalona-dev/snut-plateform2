<form method="post" action="{{ path('app_commande_semestrielle_delete', {'id': commande_semestrielle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_semestrielle.id) }}">
    <button class="btn">Delete</button>
</form>
