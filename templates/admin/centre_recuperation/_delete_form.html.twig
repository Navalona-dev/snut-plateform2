<form method="post" action="{{ path('app_admin_admin_centre_recuperation_delete', {'id': centre_recuperation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centre_recuperation.id) }}">
    <button class="btn">Delete</button>
</form>
