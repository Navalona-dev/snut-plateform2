<form method="post" action="{{ path('app_mois_projections_admissions_delete', {'id': mois_projections_admission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mois_projections_admission.id) }}">
    <button class="btn">Delete</button>
</form>
