{% extends 'base_admin.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_groupe_index') }}">back to list</a>

    <a href="{{ path('app_admin_groupe_edit', {'id': groupe.id}) }}">edit</a>

    {{ include('admin/groupe/_delete_form.html.twig') }}
{% endblock %}
