<form method="post" action="{{ path('app_admin2_admin_creni_mois_projections_admissions_delete', {'id': creni_mois_projections_admission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ creni_mois_projections_admission.id) }}">
    <button class="btn">Delete</button>
</form>
