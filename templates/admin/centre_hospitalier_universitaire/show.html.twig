{% extends 'base.html.twig' %}

{% block title %}CentreHospitalierUniversitaire{% endblock %}

{% block body %}
    <h1>CentreHospitalierUniversitaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre_hospitalier_universitaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ centre_hospitalier_universitaire.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_admin_centre_hospitalier_universitaire_index') }}">back to list</a>

    <a href="{{ path('app_admin_admin_centre_hospitalier_universitaire_edit', {'id': centre_hospitalier_universitaire.id}) }}">edit</a>

    {{ include('admin/admin_centre_hospitalier_universitaire/_delete_form.html.twig') }}
{% endblock %}
