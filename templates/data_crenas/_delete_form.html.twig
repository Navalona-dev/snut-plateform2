<form method="post" action="{{ path('app_data_crenas_delete', {'id': data_crena.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ data_crena.id) }}">
    <button class="btn">Delete</button>
</form>
